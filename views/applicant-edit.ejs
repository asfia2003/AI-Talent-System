<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Applicant</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container my-5">
  <h2 class="text-center mb-4">✏️ Edit Applicant</h2>

  <form method="POST" action="/applicants/edit/<%= applicant._id %>">
    <div class="mb-3">
      <label>Full Name</label>
      <input type="text" name="name" class="form-control" value="<%= applicant.name %>" required>
    </div>

    <div class="mb-3">
      <label>Email</label>
      <input type="email" name="email" class="form-control" value="<%= applicant.email %>" required>
    </div>

    <div class="mb-3">
      <label>Experience (years)</label>
      <input type="number" name="experience" class="form-control" value="<%= applicant.experience %>" required>
    </div>

    <div class="mb-3">
      <label>Education</label>
      <input type="text" name="education" class="form-control" value="<%= applicant.education %>" required>
    </div>

    <div class="mb-3">
      <label>Skills</label><br>
      <% const skills = ['JavaScript', 'Python', 'SQL', 'React']; %>
      <% skills.forEach(skill => { %>
        <label class="me-3">
          <input type="checkbox" name="skills" value="<%= skill %>" 
            <%= applicant.skills.includes(skill) ? 'checked' : '' %> > <%= skill %>
        </label>
      <% }) %>
    </div>

    <button type="submit" class="btn btn-primary">💾 Save Changes</button>
    <a href="/applicants" class="btn btn-secondary ms-2">Cancel</a>
  </form>
</div>
</body>
</html>
