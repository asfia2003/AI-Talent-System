<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>AI Talent System</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/css/styles.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-4">
    <h2 class="text-center mb-4">📋 All Employees</h2>

    <table class="table table-bordered table-striped shadow-sm">
      <thead class="table-dark">
        <tr>
          <th>Name</th>
          <th>Age</th>
          <th>Salary</th>
          <th>Workload (%)</th>
          <th>Performance</th>
          <th>Satisfaction</th>
          <th>Recommendation</th> <!-- updated heading -->
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% employees.forEach(emp => { %>
          <tr>
            <td><%= emp.name %></td>
            <td><%= emp.age %></td>
            <td>Rs. <%= emp.salary %></td>
            <td><%= emp.workload %>%</td>
            <td><%= emp.performance_score %></td>
            <td><%= emp.satisfaction %></td>
            <td>
              <% if (emp.trainingRecommendation === '📘 Employee Engagement Workshop') { %>
                <span class="badge bg-primary">📘 Engagement Workshop</span>
              <% } else if (emp.trainingRecommendation === '📈 Career Growth Program') { %>
                <span class="badge bg-success">📈 Growth Program</span>
              <% } else if (emp.trainingRecommendation === '💡 Skill Development Training') { %>
                <span class="badge bg-warning text-dark">💡 Skill Training</span>
              <% } else if (emp.trainingRecommendation === '🧠 Leadership Development Training') { %>
                <span class="badge bg-secondary">🧠 Leadership Course</span>
              <% } else if (emp.trainingRecommendation === '🌿 Employee Wellness Program') { %>
                <span class="badge bg-info text-dark">🌿 Wellness Program</span>
              <% } else { %>
                <span class="badge bg-light text-dark">✅ No Action Needed</span>
              <% } %>
            </td>
            <td>
              <div class="d-flex gap-2">
                <a href="/employee/edit/<%= emp._id %>" class="btn btn-sm btn-warning">Update</a>

                <form action="/employee/delete/<%= emp._id %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this employee?');">
                  <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                </form>
              </div>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</body>
</html>
