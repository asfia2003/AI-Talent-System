<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Employee</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <h2>Edit Employee</h2>
    <form action="/employee/update/<%= employee._id %>" method="POST" class="card p-3 shadow-sm">
      <input type="hidden" name="id" value="<%= employee._id %>">




      <label>Name</label>
      <input name="name" value="<%= employee.name %>" class="form-control mb-2" required>

      <label>Position</label>
      <input name="position" value="<%= employee.position %>" class="form-control mb-2" required>

      <label>Age</label>
      <input name="age" type="number" value="<%= employee.age %>" class="form-control mb-2" required>

      <label>Salary</label>
      <input name="salary" type="number" value="<%= employee.salary %>" class="form-control mb-2" required>

      <label>Workload (%)</label>
      <input name="workload" type="number" value="<%= employee.workload %>" class="form-control mb-2" required>

      <label>Tenure (years)</label>
      <input name="tenure" type="number" value="<%= employee.tenure %>" class="form-control mb-2" required>

      <label>Performance Score (1–10)</label>
      <input name="performance_score" type="number" value="<%= employee.performance_score %>" class="form-control mb-2" required>

      <label>Location (City)</label>
      <input name="location" value="<%= employee.city %>" class="form-control mb-3" required>

      <button type="submit" class="btn btn-primary w-100">Update Employee</button>
    </form>
  </div>
</body>
</html>
